package ru.privatenull.pnlibrary.api.version

/** Binary/public compatibility generation provided by this pnLibrary runtime. */
object PnLibraryApi {
    /** Current integer API generation, independent of the library semantic version. */
    const val VERSION: Int = 1
}
