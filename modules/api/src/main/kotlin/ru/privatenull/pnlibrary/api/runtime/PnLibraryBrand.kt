package ru.privatenull.pnlibrary.api.runtime

/** Stable public identity and support endpoints used across pnLibrary modules. */
object PnLibraryBrand {
    /** Organization name displayed in generated metadata and diagnostics. */
    const val ORGANIZATION = "pnFolder"

    /** Public support community URL suitable for user-facing error messages. */
    const val SUPPORT_URL = "https://discord.gg/TFdnyvMft"
}
