pnLibrary Demo Bukkit

Install pnLibrary and this plugin on a Paper/Spigot server.
Use /pndemo status, /pndemo balance, /pndemo give <amount>, and /pndemo reload.
The plugin intentionally uses pnLibrary for context, lifecycle, metrics, diagnostics,
tasks, placeholders, events, cooldowns, components, and currency operations.
